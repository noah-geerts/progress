# Progress Backend

## Quick Start

1. Ensure you have Java, Maven, and Docker installed
2. Run `docker-compose up -d` to spin up an instance of a PostgreSQL container
3. Run `mvn spring-boot:run` to start the Spring Boot REST API

## Environment Variables

Not yet configured.

## API Reference

[Find here](./API%20Reference.md)
